# D1: "From Project to Profit" heading + intro rewrite

Authorized by Brooke 2026-08-18, gave the exact heading and a draft of
the intro copy directly.

Authorized target: creation.html, #portfolio section heading + intro
paragraph only (the `<p class="section-kicker">`, `<h2 id="portfolio-title">`,
and `<p class="section-intro">` lines). Does NOT touch the OWNER-LOCKED
`.studio-showcase` demo component below it — that was never in scope,
the lock only wraps the demo itself, not the heading/intro above it.

Change: heading becomes "From Project to Profit"; intro paragraph keeps
the original "working record of ideas, decisions, effort, finished work,
and reflection—kept under parent or guardian control" sentence and adds
a second sentence bridging to the storefront demo (portfolio can become
a real business + free Rebel Ranch Marketplace listing), cleaned up from
Brooke's draft for typos/grammar only, same intent and phrasing kept.

Do not commit, push, publish, or deploy.
